<% cart.each do |product| %>
  <%= product %>
<% end %>

<%= form_for(controller:"products", action:"add", method:"post") do |f| %>
  <%= f.label "product" %>
  <%= f.text_field :product %>
  <%= f.submit %>
<% end %>
